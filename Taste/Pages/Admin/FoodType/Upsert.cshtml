@page
@model Taste.Pages.Admin.FoodType.UpsertModel
@{
}

<form method="post" >
	<div class="container">
		<div class="row px-2 mx-2 border py-4 " style="border-radius: 10px; ">
			<div asp-validation-summary="ModelOnly" class="text-danger"></div>
		<div class="col-12 px-3" style="border-bottom: 1px solid #325d88">
			<h2 class="text-primary">@(Model.foodType.Id != 0 ? "Update" : "Create") Category</h2>
		</div>
			@if(Model.foodType.Id != 0)
			{
				<input type="hidden" asp-for="foodType.Id">
			}
			<div class="col-8 pt-3">
			<div class="form-group row mt-3">
				<div class="col-3">
					<label asp-for="foodType.Name"></label>
				</div>
				<div class="col-9">
					<input class="form-control" asp-for="foodType.Name" />
					<span class="text-danger" asp-validation-for="foodType.Name"></span>
				</div>
			</div>
			
			<div class="form-group row mt-3">
				<div class="col-9 offset-3">
					<div class="row">
						<div class="col-6">
							<button type="submit" class="btn btn-success form-control" > @(Model.foodType.Id != 0 ? "Update" : "Create")</button>
						</div>
						<div class="col-6">
							<a class="btn btn-primary form-control" asp-page="./Index">Back to List</a>
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>
</div>
</form>